type Cliente {
  id: ID!,
  nome: String!,
  cpf: String!,
  pets: [Pet!]
}

type Mutation {
  adicionarCliente(nome: String!, cpf: String!): Cliente!
  atualizarCliente(id: ID!, nome: String!, cpf: String!): Cliente!
  deletaClientePorId(id: ID!): String!
}

type Query {
  listaClientes: [Cliente]!
  consultaClientePorId(id: ID!): Cliente!
}